<Window x:Class="ZippyClip.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZippyClip"
        xmlns:items="clr-namespace:ZippyClip.Items"
        mc:Ignorable="d"
        Title="ZippyClip" Height="450" Width="285" Background="White" WindowStyle="ToolWindow" Loaded="Window_Loaded">
    <Window.Resources>
        <DataTemplate DataType="{x:Type items:ImageItem}">
            <Border Margin="3" Padding="6" BorderBrush="DarkGray" Background="GhostWhite" CornerRadius="6" Height="64">
                <StackPanel Orientation="Horizontal">
                    <Button Click="ButtonCopyItem_Click" Width="16" Height="32" Margin="2" BorderThickness="0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
                        <Image Source="Resources/copy-document-small.png" StretchDirection="DownOnly" SnapsToDevicePixels="True" />
                    </Button>
                    <Button Click="ButtonPreviewItem_Click" Width="16" Height="32" Margin="2" BorderThickness="0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
                        <Image Source="Resources/magnifying-glass-small.png" StretchDirection="DownOnly" SnapsToDevicePixels="True" />
                    </Button>
                    <Image Source="{Binding Image}" HorizontalAlignment="Left" />
                </StackPanel>
            </Border>
        </DataTemplate>
        <DataTemplate DataType="{x:Type items:TextItem}">
            <Border Margin="3" Padding="6" MaxHeight="150" BorderBrush="DarkGray" Background="GhostWhite" CornerRadius="6">
               <TextBlock Text="{Binding Text}" />
            </Border>
        </DataTemplate>
        <DataTemplate DataType="{x:Type items:UriItem}">
            <Border Margin="3" Padding="6" MaxHeight="150" BorderBrush="DarkGray" Background="GhostWhite" CornerRadius="6">
                <TextBlock>
                    <Hyperlink NavigateUri="{Binding Uri}" RequestNavigate="Hyperlink_RequestNavigate">
                        <TextBlock Text="{Binding Uri}" />
                    </Hyperlink>
                </TextBlock>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ListBox ItemsSource="{Binding Path=ClipboardHistory}" SelectedItem="{Binding Path=SelectedItem}" MouseDoubleClick="ListBox_MouseDoubleClick" HorizontalContentAlignment="Stretch" ScrollViewer.CanContentScroll="False" Background="#FF272727">
        </ListBox>
    </Grid>
</Window>
